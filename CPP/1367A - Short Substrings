#include <iostream>
using namespace std;

int main() {
	int r;
	cin >> r;
	for (int j = 0; j < r; j++) {
		string s; cin >> s;
		for (int i = 0; i < s.size(); i += 2)cout << s[i]; 
		cout << s[s.size() - 1] << "\n";
	}
}
